name: YiffBukkit
main: de.doridian.yiffbukkitsplit.YiffBukkit
version: 1.0.0
load: STARTUP
depend: [WorldEdit]
softdepend: [dynmap, LogBlock]
commands:
    yb:
        description: Runs any YiffBukkit command (used for console)
        permission: yiffbukkit.no_one_has_this_ever
        usage: /<command>